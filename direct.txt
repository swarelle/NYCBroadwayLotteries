// Use jQuery
let jq = document.createElement("script");
jq.src = "https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js";
document.getElementsByTagName("head")[0].appendChild(jq);

// Store personal information in variables
let firstName,
    lastName,
    email,
    zipCode,
    birthDay,
    birthMonth,
    birthYear;

/*
 * Prompts user for information used to fill out forms
 */
function setupInfo() {
  firstName = prompt("First name");
  lastName = prompt("Last name");
  email = prompt("E-mail address");
  zipCode = prompt("Zip code");
  birthDay = parseInt(prompt("Birth day (integer)"));
  birthMonth = parseInt(prompt("Birth Month (integer)"));
  birthYear = parseInt(prompt("Birth year (4-digit integer)"));
}

/*
 * Fills out the broadwaydirect.com lottery form using the given information
 */
function fillForm() {
  // $("#dlslot-field-name_first").
}

setupInfo();

